import PreviewView from "@/views/PreviewView";

export const metadata = {
  title: "Preview (illustrative) · The Equity Gap",
  description:
    "Illustrative mockups of future views, built on fabricated placeholder data — not real, not verified, not part of the dashboard's findings.",
};

export default function Page() {
  return <PreviewView />;
}
